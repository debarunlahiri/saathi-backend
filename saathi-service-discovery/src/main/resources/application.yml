server:
  port: 8761

spring:
  application:
    name: saathi-service-discovery

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
