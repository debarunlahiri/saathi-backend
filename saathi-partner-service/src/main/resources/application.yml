server:
  port: 8083
spring:
  application:
    name: saathi-partner-service
  datasource:
    url: ${PARTNER_DB_URL:jdbc:postgresql://localhost:5432/saathi_partner}
    username: ${POSTGRES_USER:saathi}
    password: ${POSTGRES_PASSWORD:saathi}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka}
